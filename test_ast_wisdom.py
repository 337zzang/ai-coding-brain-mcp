
import os
import sys
import os  # 중복 import

def bad_function(a, b, c, d, e, f):  # 많은 인자
    unused = 10  # 미사용 변수
    
    # 복잡한 중첩
    if a:
        if b:
            if c:
                if d:
                    print("너무 복잡해요")
    
    # TODO: 리팩토링 필요
    console.log("이건 JavaScript 아닌데...")  # console 사용
    
    very_very_very_very_very_very_very_very_very_very_very_very_long_line = "이 줄은 100자를 넘습니다"   
    
flow_project("직접 호출하면 안돼요")  # direct_flow

print("끝")
